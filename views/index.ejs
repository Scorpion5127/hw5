<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Star Wars Quote App</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 20px;
      padding: 0;
    }

    /* Use grid layout for the container */
    .container {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      grid-gap: 20px;
      background-color: #f2f2f2; /* Grey background color */
      padding: 20px;
    }

    /* Style for each grid item */
    .item {
      padding: 20px;
      border: 1px solid #ccc;
      border-radius: 5px;
      background-color: white;
    }

    h1 {
      color: #003366;
    }
    form {
      /* Removed max-width to make it span the column */
      display: flex;
      flex-direction: column;
      margin-bottom: 20px;
      padding: 20px; /* Add padding for the form */
      border: 1px solid #ccc; /* Add border for the form */
      border-radius: 5px; /* Add border-radius for rounded corners */
      background-color: #f9f9f9; /* Background color for the form */
    }
    input {
      padding: 8px;
      margin-bottom: 10px;
    }
    button {
      padding: 8px 15px;
      background-color: #003366;
      color: white;
      border: none;
      cursor: pointer;
    }
    button:hover {
      background-color: #002147;
    }
    h2 {
      color: #003366;
      margin-top: 0;
      margin-bottom: 10px;
    }
    .quotes {
      list-style: none;
      background-color: white; /* Grey background color */
      padding: 0;
    }
    .quote {
      border: 1px solid #ccc;
      border-radius: 5px;
      padding: 10px;
      margin: 10px 0;
    }
    .quote span {
      font-weight: bold;
    }
    #update-button,
    #delete-button {
      margin-top: 10px;
    }
    #message {
      margin-top: 10px;
      color: #cc0000;
    }

    /* Style for the new-div */
    #new-div {
      padding: 20px;
      border: 1px solid #ccc;
      border-radius: 5px;
      background-color: #f2f2f2; /* Grey background color */
      margin-top: 20px; /* Add margin to separate from the first row */
    }
  </style>
</head>

<body>
  <h1>May Node and Express be with you.</h1>

  <div class="container">
    <!-- Form -->
    <div class="item">
      <form action="/quotes" method="POST">
        <input type="text" placeholder="Name" name="name" />
        <input type="text" placeholder="Quote" name="quote" />
        <button type="submit">Submit</button>
      </form>
    </div>

    <!-- Darth Vader invades! -->
    <div class="item" id="invades">
      <h2>Darth Vader invades!</h2>
      <p>Replace the first Yoda's quote with a quote written by Darth Vader</p>
      <button id="update-button">Replace Yoda's quote</button>
    </div>

    <!-- Remove Darth Vader! -->
    <div class="item" id="remove">
      <h2>Remove Darth Vader!</h2>
      <p>Delete one Darth Vader's quote. Does nothing if there are no more Darth Vader's quotes.</p>
      <button id="delete-button">Delete Darth Vader's quote</button>
      <div id="message"></div>
    </div>
  </div>

  <!-- Quotes -->
  <div id="new-div"> <!-- This div is outside the grid layout -->
    <h2>Quotes</h2>
    <ul class="quotes">
      <!-- Loop through quotes -->
      <% for (var i = 0; i < quotes.length; i++) { %>
      <li class="quote">
        <!-- Output name from the iterated quote object -->
        <span><%= quotes[i].name %></span>:
        <!-- Output quote from the iterated quote object -->
        <span><%= quotes[i].quote %></span>
      </li>
      <% } %>
    </ul>
  </div>

  <script src="/main.js"></script>
</body>
</html>
